server {
    listen 80;
    server_name wulab.yourdomain.com;   # เปลี่ยนเป็น domain จริง

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass         http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
        client_max_body_size 20M;
    }

    location /_next/static/ {
        proxy_pass http://127.0.0.1:3001;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}

# HTTPS — uncomment after: certbot --nginx -d wulab.yourdomain.com
# server {
#     listen 443 ssl http2;
#     server_name wulab.yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/wulab.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/wulab.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass         http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade           $http_upgrade;
#         proxy_set_header   Connection        "upgrade";
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60s;
#         client_max_body_size 20M;
#     }
#
#     location /_next/static/ {
#         proxy_pass http://127.0.0.1:3001;
#         add_header Cache-Control "public, max-age=31536000, immutable";
#     }
# }
